# Third-party ROS packages (clone separately — see CLAUDE.md for sources)
jackal/
virtual_maize_field/

# Lab papers — kept locally, not published to GitHub
Papers/

# Catkin build artifacts (lives outside src/ anyway)
../build/
../devel/
../logs/
../install/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Model weights — large binary, distribute separately
*.pt
*.pth
*.onnx

# ROS generated files
*.pyc

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store

# Windows metadata files created when downloading in Windows then accessing via WSL
*:Zone.Identifier

# Derived: regenerate with `zip -j AIAgNav_Overleaf.zip AIAgNav_Overleaf.tex`
AIAgNav_Overleaf.zip
